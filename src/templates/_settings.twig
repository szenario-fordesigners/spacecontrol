{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
  label: 'Time in seconds between sending mails',
  name: 'mailTimeThreshold',
  value: settings.mailTimeThreshold
}) }}

{{ forms.textField({
  label: 'Max disk usage in percent',
  name: 'diskLimitPercent',
  value: settings.diskLimitPercent,
  type: 'number',
  min: 0,
  max: 100
}) }}

{{ forms.textField({
  label: 'Admin Recipients',
  name: 'adminRecipients',
  value: settings.adminRecipients,
}) }}

{{ forms.textField({
  label: 'Client Recipients',
  name: 'clientRecipients',
  value: settings.clientRecipients,
}) }}