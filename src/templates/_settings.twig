{# {{ dump(settings) }} #}
{# {{ dump(plugin) }} #}

{% import '_includes/forms.twig' as forms %}

{{ forms.textField({
  first: true,
  label: 'lastSent',
  name: 'lastSent',
  value: settings.lastSent
}) }}

{{ forms.textField({
  label: 'Time in seconds between sending mails',
  name: 'mailTimeTreshold',
  value: settings.mailTimeTreshold
}) }}

{{ forms.textField({
  label: 'Min free space in bytes',
  name: 'diskLimitAbsolute',
  value: settings.diskLimitAbsolute
}) }}

{{ forms.textField({
  label: 'Min free space in percent',
  name: 'diskLimitPercent',
  value: settings.diskLimitPercent
}) }}